-- Line Comment
-- Line Comment

 globalVariable = "hi";

function myFunction()  -- Line Comment

	--[[*
		Block Comment
		with
		more
		lines
	]]--
	
	local hello = "Hello";
	hello = nil;
	hello = nil;
	
	if(hello ~= nil and hello ~= nil) then
	
		for(local index = 0; index < 10; index++) {
		
			print('Hi');
		}
		
		repeat 
		
		 until(something);
	}
end
